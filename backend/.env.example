# Server Config
PORT=3000
NODE_ENV=development

# Sui Network Config
SUI_NETWORK=testnet
SUI_RPC_URL=https://fullnode.testnet.sui.io:443

# STEP Token Config
# Get these from deploying step-token contract
SUI_PACKAGE_ID=YOUR_PACKAGE_ID
SUI_TREASURY_CAP_ID=YOUR_TREASURY_CAP_ID

# Admin wallet (mnemonic phrase)
# WARNING: Keep this secret! Never commit real keys!
SUI_ADMIN_MNEMONIC=your twelve word mnemonic phrase here replace with real one

# Reward Config
REWARD_PER_STEP=1000000
MIN_STEPS_FOR_CLAIM=10
MAX_CLAIMS_PER_DAY=3

# Database
DB_PATH=./stepsign.db
